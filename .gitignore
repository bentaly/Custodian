node_modules/
dist/
build/
.output/
.vinxi/
.wrangler/
.env
.env.local
*.tsbuildinfo

# Compiled JS artifacts (tsc output alongside TS sources)
src/**/*.js
drizzle/schema.js
drizzle.config.js
/vite.config.js
.DS_Store

# TanStack Router (auto-generated, committed but here for reference)
# app/routeTree.gen.ts

# Local secrets (admin API token etc.)
.secrets.local*

# One-off form-header harvesting helper (see file header)
scripts/extract-form-headers.py

# TanStack Router scratch dir (temp routeTree copies during regeneration)
.tanstack/
